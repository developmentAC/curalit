# What is this directory?

In this directory are files that contain run scripts and information that the developers use to run the tool. Many of these commands are actually printed out by CuraLit after completing each step.
